{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run Pytest",
            "type": "python",
            "request": "launch",
            "program": "${workspaceFolder}/venv/bin/pytest",
            "args": ["${file}"],
            "env": {
                "PYTHONPATH": "${workspaceFolder}"
            }
        }
    ]
}
